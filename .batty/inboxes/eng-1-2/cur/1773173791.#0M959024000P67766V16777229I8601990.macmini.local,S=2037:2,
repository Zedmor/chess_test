{"from":"manager","to":"eng-1-2","body":"PRIORITY: Finish Task 6 — board.py + test_board.py\n\nSTATUS: Your src/board.py in the working copy looks GOOD but is NOT committed. The old committed version (with W_PAWN etc.) is outdated. test_board.py does NOT exist yet. The test_move_ordering.py has import errors and should be deleted for now.\n\nDO THIS IN ORDER:\n\n1. DELETE tests/test_move_ordering.py and src/move_ordering.py (they belong to a different task and have import errors)\n\n2. COMMIT src/board.py as-is — it looks correct with proper imports from src.constants\n\n3. CREATE tests/test_board.py with these REQUIRED tests:\n   - test_default_fen: Board() loads starting position, get_fen() returns standard FEN\n   - test_custom_fen_roundtrip: Board(fen).get_fen() == fen for several FENs\n   - test_make_unmake_reversible: make_move then unmake_move restores original FEN (pawn push, capture, castling, en passant, promotion)\n   - test_pawn_double_push_sets_ep: After e2e4 double pawn push, ep_square == 20 (e3)\n   - test_castling_rights_update: Moving king clears both sides; moving rook clears that side\n   - test_castling_move: Kingside castle: king e1->g1, rook h1->f1\n   - test_en_passant_capture: EP capture removes captured pawn from correct square\n   - test_promotion: Pawn reaching 8th rank becomes correct piece\n   - test_is_attacked_by_knight: Knight attacks correct squares\n   - test_is_attacked_by_sliding: Bishop/rook attacks along rays, blocked by pieces\n   - test_is_in_check: Detected when king is attacked\n   - test_king_sq_tracking: king_sq updated on king move and castling\n   - test_halfmove_clock: Resets on pawn/capture, increments otherwise\n   - test_position_history: position_history grows on make, shrinks on unmake\n\n4. RUN: python -m pytest tests/test_board.py -v — ALL must pass\n\n5. COMMIT test_board.py\n\nUse encode_move() from src.constants to create moves for testing. See CLAUDE.md for all constants and function signatures.","msg_type":"assign","timestamp":1773173791}