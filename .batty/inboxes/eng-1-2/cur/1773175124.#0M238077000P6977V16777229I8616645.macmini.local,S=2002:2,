{"from":"manager","to":"eng-1-2","body":"TASK 12: Implement src/uci.py + main.py + tests/test_uci.py — UCI protocol handler\n\nSearch.py is now on main. Pull main first: git merge main\n\nCreate src/uci.py with uci_loop():\n- Read stdin line by line, handle UCI commands\n- 'uci' -> print 'id name ChessEngine\\nid author Team\\nuciok'\n- 'isready' -> print 'readyok'\n- 'ucinewgame' -> reset board\n- 'position startpos [moves e2e4 ...]' -> set position, apply moves\n- 'position fen <fen> [moves ...]' -> set from FEN, apply moves\n- 'go wtime X btime X' -> time_limit = own_time / 30 (in seconds), search, print 'bestmove <uci>'\n- 'go movetime X' -> time_limit = X / 1000, search, print 'bestmove <uci>'\n- 'go depth X' -> search with large time but cap at depth X\n- 'quit' -> sys.exit()\n\nIntegration:\n- from src.board import Board\n- from src.constants import parse_uci_move, move_to_uci\n- from src.opening_book import get_book_move\n- from src.search import search\n- On 'go': try book_move = get_book_move(board) first. If None, use search(board, time_limit)\n- Use sys.stdout.write() + flush() for output (UCI requires immediate flushing)\n\nCreate main.py:\n    from src.uci import uci_loop\n    if __name__ == '__main__':\n        uci_loop()\n\nCreate tests/test_uci.py (mock stdin/stdout with io.StringIO and monkeypatch):\n- test_uci_identification: sends 'uci\\nquit', output contains 'id name' and 'uciok'\n- test_isready: sends 'isready\\nquit', output contains 'readyok'\n- test_position_startpos: sends 'position startpos\\nisready\\nquit', gets readyok\n- test_position_with_moves: sends 'position startpos moves e2e4 e7e5\\nisready\\nquit'\n- test_position_fen: sends 'position fen <custom fen>\\nisready\\nquit'\n- test_go_bestmove: sends 'position startpos\\ngo movetime 500\\nquit', output contains 'bestmove' followed by valid UCI move\n- test_quit_exits: loop terminates on 'quit'\n\nRun: python -m pytest tests/test_uci.py -v","msg_type":"assign","timestamp":1773175124}