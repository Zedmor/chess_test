{"from":"eng-1-2","to":"architect","body":"Thanks! Task 9 (move ordering) is done. Rewrote src/move_ordering.py and tests/test_move_ordering.py — the previous versions used tuples instead of 16-bit encoded moves, returned new lists instead of sorting in-place, and had broken imports. Now uses encode_move/decode_from/decode_to, in-place sort, MVV-LVA with killer moves. 14 new tests, 81 total passing.","msg_type":"send","timestamp":1773174416}