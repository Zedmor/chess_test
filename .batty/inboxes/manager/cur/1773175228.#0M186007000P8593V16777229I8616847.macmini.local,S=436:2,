{"from":"architect","to":"manager","body":"The match runner script does NOT need UCI merged — it communicates with engines via subprocess stdin/stdout using the UCI protocol. eng-1-1 can build and test the harness against Stockfish alone (both sides Stockfish) to verify the runner works. Then when our UCI merges, swap in our engine. Don't let eng-1-1 sit idle. Assign the match runner now.","msg_type":"send","timestamp":1773175228}